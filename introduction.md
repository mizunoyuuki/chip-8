CHIP-8
ゲームが動かせるらしい。


### 「どうやって画面に絵を描くか」
=> ディスプレイとスプライト
内臓フォントデータ

CHIP8のオリジナル画面サイズは横64ピクセル・縦32ビピクセルのモノクロ
座標系: 左上(0,0) 右下(63, 31)になる。

スプライト:
CHIP8は「スプライト」と呼ばれるデータの塊を使って画面に文字を描く。
スプライトの横幅は常に8ピクセル(1バイト)で、縦幅は最大15ピクセル(15バイト) 


### ディレイタイマーとサウンドタイマー

CHIP-8は、ディレイタイマーとサウンドタイマーという二つのタイマーを提供する

- ディレイタイマーは、ディレイタイマーレジスタ(DT)の値が0以外の時にアクティブになる。
このタイマーは、69Hzの速度で、DTの値から1をひく。という処理だけを行う。
DTが0になると非アクティブになる。

- サウンドタイマーは、サウンドタイマーレジスタ(ST)の値が0以外の時に、アクティブになる。
このタイマーも同じく、60Hzの速度で減算する。STの値が０より大きい間は、CHIP-8のブザーがなり続ける。
STが0になると、サウンドタイマーは非アクティブになる。(=> 音が止まる)

音は、CHIP-8は、ビープ音を鳴らすだけ。

### CHIP8の命令

CHIP8の命令は、計算、グラフィック、フロー演算、の機能を含む36種類の命令がある。
全ての命令は2バイト長で、最上位バイト(most-significant-byte)が先に格納される。
また、メモリ上では、各命令の最初のバイトが偶数アドレスに配置されている必要がある。

=> ようは、chip8はビックエンディアンでデータが並んでいる。

### リトルエンディアン・ビックエンディアン
エンディアンとは、複数バイトのデータをメモリ上にどのような順序で並べるかという話。

```
uint32_t value = 0x1234578;

=> 0x12 0x34 0x56 0x78 という四つのバイトをどういう順番で並べるか
```
ビックエンディアン
最上位バイト(一番大きい桁)を先に(低いアドレス)格納する方式

- ネットワークプロトコル(TCP/IPのヘッダなど) ネットワークバイトオーダーとも呼ばれる。
- 古いMac
- 多くのRISCプロセッサ(初期のMIPS)
- JVM

リトルエンディアン
最下位バイトを先に格納する方法

```
0x12345678というデータがあった場合

アドレス: 0x1000  0x1001  0x1002  0x1003
データ  : 0x78    0x56    0x34    0x12
```

といった感じ。

リトルエンディアンは人間の感覚的には逆順に見えるが、CPUにとっては都合がいい時がある。

- x86/x64
- ARM
- RISC-V

=> リトルエンディアンって何かいいことあるの？？
- 型のキャストが楽: 例えば、uint32_tの値をuint8_t*でアクセスするとすれば、自動的にその変数にアクセスすると最下位バイトが取れる。
- 加算・減算などの算術演算で、桁上がりの処理を低い位から始めることができるので、ハードウェア実装が楽。
足し算って、小さいくらいからやるよねって話、低いバイトが先に取れた方がいい。

#### 0nnn SYS addr
nnn番地にある機械語ルーチンへジャンpsうる

#### 00E0 CLS
ディスプレイをクリアする

#### 00EE RET
サブルーチンから復帰する

#### 1nnn JUMP addr
nnn番地へジャンプする

#### 2nnn CALL addr
nnn番地のサブルーチンを呼び出す

.....これが続く

